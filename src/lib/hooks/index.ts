// Custom hooks for YoYo Champion

// Hydration hooks for SSR support
export { useHydration, useAllHydrated } from "./use-hydration";

// Client-only rendering hook
export { useMounted, ClientOnly, type ClientOnlyProps } from "./use-mounted";
